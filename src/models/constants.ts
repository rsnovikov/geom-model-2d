export const MIN_LINE_WIDTH = 0.5;
export const MAX_LINE_WIDTH = 1.4;
export const LINE_WIDTH_STEP = 0.1;
export const BASE_LINE_WIDTH = 3;
export enum LineTypes {
  SOLID = "SOLID",
  DOTTED = "DOTTED",
  STREAK_DOTTED = "STREAK_DOTTED",
}

export enum CoordSystemTypes {
  POLAR = "POLAR",
  CARTESIAN = "CARTESIAN",
}
